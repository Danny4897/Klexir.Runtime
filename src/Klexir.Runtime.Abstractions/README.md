# Klexir.Runtime.Abstractions

Public bytecode contract for the Klexir runtime: `OpCode`. This is the interface `Klexir.Lang` compiles against. Implementation (the interpreter, memory model, scheduler) is not part of this package.
